// import MethodOverriding.*;.CalcMain;

import MethodOverriding.CalcMain;



// just to understand the package structire if we want to access the files from differnet packages them we have to import ther file from the particular packages as we have written above import statement

public class main {
    CalcMain cm = new CalcMain();
}
